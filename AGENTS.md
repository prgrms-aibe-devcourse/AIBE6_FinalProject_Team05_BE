# AGENTS.md

이 문서는 이 저장소에서 코드를 분석하거나 수정하는 모든 AI 코딩 도구(Claude Code, GitHub Copilot Coding Agent,
OpenAI Codex 등)가 공통으로 따라야 할 작업 규칙을 정의한다. 이 저장소는 **백엔드 전용 저장소**이며, 프론트엔드는
별도 저장소에서 관리된다. 따라서 이 문서는 프론트엔드 관련 규칙을 포함하지 않는다.

## 1. 핵심 원칙

- 사용자가 명시적으로 다른 언어를 요청하지 않는 한, 설명·요약·질문·최종 응답은 한국어로 작성한다.
- 사용자가 요청한 범위만 수정한다. 요청받지 않은 대규모 리팩터링, 파일 이동/이름 변경, 기술 교체를 하지 않는다.
- 코드를 수정하기 전에 관련 파일과 기존 구현을 먼저 확인한다.
- 기존 프로젝트 구조, 네이밍 컨벤션, 코드 스타일을 따른다.
- 새 기능을 구현하기 전에 유사하거나 기존에 존재하는 기능이 있는지 먼저 검색한다.
- 추측만으로 구현하지 않는다. 불확실한 부분은 코드, 설정, 테스트, 문서에서 확인한다.
- API 응답/요청 스키마 변경이 프론트엔드(별도 저장소)에 영향을 줄 수 있는 경우, 최종 응답에 그 영향을 명시한다.
- 임시 코드, 의미 없는 TODO 주석, 사용하지 않는 파일, 죽은 코드를 남기지 않는다.
- 최종 응답에는 변경 이유와 검증 방법을 포함한다.
- 사용자가 상세 설명을 요청하지 않는 한 응답은 간결하고 실용적으로 작성한다.

## 2. 프로젝트 구조

```
AIBE6_FinalProject_Team05_BE/   # 저장소 루트 (백엔드 전용)
├─ Pokade/                      # Spring Boot 프로젝트 (module: Pokade, package root: com.pokade)
│  ├─ src/main/java/com/pokade/
│  ├─ src/test/java/com/pokade/
│  ├─ src/main/resources/       # application.yaml, application-dev.yaml, application-prod.yaml
│  ├─ build.gradle.kts / settings.gradle.kts
│  └─ docker-compose.yml        # 로컬 Postgres/Redis
├─ .github/                     # 이슈/PR 템플릿
├─ AGENTS.md                    # 이 문서 (AI 도구 공통 규칙)
└─ CLAUDE.md                    # Claude Code 진입 지침 (빌드/실행 명령, 아키텍처 개요)
```

- 실제 저장소 구조가 이 문서와 다르면 현재 저장소 구조를 우선한다.
- 하위 디렉터리에 더 구체적인 AGENTS.md가 있으면 그것을 우선 적용한다.

## 3. 코드 수정 전 필수 워크플로우

1. 사용자 요청의 의도와 정확한 변경 범위를 파악한다.
2. 관련 파일과 유사 구현을 검색한다.
3. 호출 관계와 의존성을 확인한다.
4. API, 데이터베이스, 인증/인가, 상태 관리에 미치는 영향을 점검한다.
5. 합리적인 범위 내에서 최소한의 변경을 구현한다.
6. 적절한 빌드/테스트/린트 또는 수동 검증 절차를 실행하거나 식별한다.
7. 변경된 파일, 주요 동작 변화, 남은 고려사항을 요약한다.

관련 구현을 찾을 수 없을 때는 새 구조를 만들기 전에 다음을 먼저 점검한다: 기존 패키지 구조, 공통 API 응답 포맷,
예외 처리 컨벤션, 인증/인가 컨벤션, 데이터베이스 스키마 관리 방식, 기존 테스트 스타일.

## 4. 공통 코딩 규칙

- 함수와 클래스는 하나의 명확한 책임만 갖도록 한다.
- 기존 공통 유틸리티, 함수, 추상화를 재사용한다.
- 지나치게 긴 함수는 의미 단위로 분리한다.
- 불명확한 축약어보다 의미가 드러나는 이름을 사용한다.
- 하드코딩된 숫자/문자열/URL은 상수나 설정으로 옮길지 검토한다.
- 사용하지 않는 import, 변수, 함수, 주석, 파일을 제거한다.
- 코드를 그대로 반복하는 주석은 작성하지 않는다. 비즈니스 규칙이나 설계 결정처럼 코드만으로 드러나지 않는
  내용일 때만 주석을 작성한다.
- 빈 catch 블록으로 예외를 숨기지 않는다.
- 비밀번호, 액세스/리프레시 토큰, 시크릿, 개인정보를 로그에 남기지 않는다.
- 기존 포맷터/린터 설정을 따른다.
- 기존 스택으로 합리적으로 해결할 수 없을 때만 새 라이브러리를 추가한다.
- 의존성을 추가할 때는 목적, 영향 범위, 대안을 설명한다.

## 5. 데이터베이스 스키마 변경 (Flyway)

이 저장소는 Flyway로 스키마를 관리한다(`Pokade/src/main/resources/db/migration/`). **`schema.sql`은 더 이상 존재하지 않으며 되살리지 않는다.**

- 새 테이블/컬럼 등 스키마 변경이 필요하면 `db/migration/V{다음 번호}__설명.sql` 파일을 새로 추가한다. 번호는 그 폴더의 현재 최댓값 + 1이어야 하며, 다른 진행 중인 PR과 번호가 겹치지 않는지 머지 직전에 다시 확인한다.
- 이 파일을 포함해 커밋하면 끝이다. **PR을 머지하기 전에 프로덕션 DB에 별도로 psql을 실행할 필요가 없다** — 배포된 앱이 부팅할 때 아직 적용되지 않은 마이그레이션을 자동으로 실행한다(`FlywayMigrationListener`가 스프링 빈 등록 없이 직접 실행 — Boot의 Flyway 자동설정을 그대로 켜면 JPA와 순환 의존 버그가 나서 우회한 것이니 이 리스너를 건드리지 않는다).
- **이미 머지되어 배포된 마이그레이션 파일의 내용은 절대 수정하지 않는다.** Flyway가 체크섬으로 검증하므로, 내용을 바꾸면 다음 배포 때 checksum mismatch 에러로 부팅이 실패한다. 잘못된 내용을 고치고 싶으면 그걸 고치는 새 번호의 마이그레이션 파일을 추가한다.
- 로컬 dev(`docker compose up -d`로 띄운 Postgres)와 테스트(Testcontainers)에도 동일하게 자동 적용되므로, 로컬에서 스키마를 손으로 맞출 필요가 없다.